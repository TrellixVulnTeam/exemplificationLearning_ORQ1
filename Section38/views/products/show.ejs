<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %></title>
</head>
<body>
    <h1><%= product.name %></h1>
    <ul>
        <li><%= product.price %></li>
        <li><%= product.category %> </li>
    </ul>

    <form action = "/products/<%= product._id %>" method = "post">
        <label for = "name">Name of Item</label>
        <input type="text" name="name" placeholder = <%= product.name %>>
        <br>
        <label for = "price">Price of Item</label>
        <input type="number" name="price" placeholder="<%= product.price %>">
        <br>
        <select name = "category" id = "category">
            <option value="fruit" <% if(product.category === "fruit") { %> selected <% } %>>fruit</option>
            <option value="vegetable" <% if(product.category === "vegetable") { %> selected <% } %>>vegetable</option>
            <option value="dairy" <% if(product.category === "dairy") { %> selected <% } %>>dairy</option>
        </select>
        <br>
        <button>Submit</button>
    </form>
    <br>
    <form action="/products/<%= product._id %>?_method=delete" method = "post">
        <button>Delete</button>
    </form>

    <p><a href = "/products">All Products</a></p>
</body>
</html>